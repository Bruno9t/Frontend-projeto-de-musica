import styled from 'styled-components'

const Title = styled.h1`
  font-size: 50px;
  color: blue;
  animation: frame1 ease-in-out infinite;
`

const Container = styled.div`
  text-align: center;
`

export { Title, Container }
